!(function(window){

    //-- load page --//
    app.Bootstrapper.load({
        callback: init,
        template: "pinky",
        profile_id:"educatricesimona-debattistis"
    });

    /**
     * Initialize page with custom scripts
     */
    function init(model){

    }

})(this);